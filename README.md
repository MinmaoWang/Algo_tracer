This repository contains the implementation of Algo_tracer, and all components have successfully passed the provided tests.

For validation, we include two external repositories as test targets.

To execute the pipeline, you can directly run Agentic Code Reader. We provide a script (run.bash) for convenience. Please make sure to export the required API environment variables before running. We use the DeepSeek API in our setup; you can obtain an API key from https://platform.deepseek.com/.